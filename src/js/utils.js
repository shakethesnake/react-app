

const Utils = {
    db: {
        test: function () {
            // var client = this.client;
            // if (client) {
            //     client.connect(function (err) {
            //         if (err) {
            //             return console.log(err);
            //         }
            //         console.log(client.query('SELECT NOW() AS "THE TIME"'));
            //     })
            // }
        }
    }
};

export default Utils;
